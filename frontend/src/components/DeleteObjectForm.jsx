const DeleteObjectForm = ({ rowObject, backendURL, refreshObject }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteObjectForm;